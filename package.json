{
  "name": "mediago-drama",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "task build",
    "build:server": "task build:server",
    "prepare-agent": "task prepare-agent",
    "dev": "task dev",
    "dev:server": "task dev:server",
    "build:workspace": "task workspace:build",
    "workspace:dev": "task workspace:dev",
    "dev:desktop": "task dev:desktop",
    "build:desktop": "task build:desktop",
    "check:go": "task -d packages/core check && task -d packages/mcp check && task -d services/server check",
    "prepare": "husky",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "latest",
    "@commitlint/cli": "latest",
    "@commitlint/config-conventional": "latest",
    "husky": "latest"
  }
}

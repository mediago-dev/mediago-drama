# dependencies
node_modules

# build output
dist
bin
coverage
packages/server/internal/app/docs/

# local media workspace state
media.workspace.json
.mediago-drama/
.media-cli/
/documents/
/projects/

# environment
.env

# secrets — private keys must NEVER be committed
# (the .secrets/.gitignore inside the dir is the primary defense; this is
# a belt-and-suspenders entry for the workspace root)
.secrets/keys/

# design-sync (claude.ai/design) — staged scripts, build output, machine state.
# Durable inputs under .design-sync/ (config.json, NOTES.md, conventions.md,
# previews/, overrides/) are NOT ignored and should be committed.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# misc
.DS_Store
.env.*
!.env.example
.claude/settings.local.json
.claude/worktrees/

/tmp
.cache
.commercial/
